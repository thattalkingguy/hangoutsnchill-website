export default function Sidebar() {
  const items = [
    "Dashboard",
    "Marketplace",
    "Academy",
    "Anonymous Chat",
    "Nestuge AI",
    "Earnings",
    "Community",
    "Settings",
  ];

  return (
    <aside className="w-64 min-h-screen bg-white border-r p-6">
      <h1 className="text-2xl font-bold text-blue-600">
        HangoutsNChill
      </h1>

      <nav className="mt-10 space-y-2">
        {items.map((item) => (
          <button
            key={item}
            className="w-full rounded-lg px-4 py-3 text-left hover:bg-blue-50 hover:text-blue-600"
          >
            {item}
          </button>
        ))}
      </nav>
    </aside>
  );
}